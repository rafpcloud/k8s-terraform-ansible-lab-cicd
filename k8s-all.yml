---
  - name: Adiciona  k8s repo
    hosts: all
    sudo: yes 
    tasks:
      - name: Add repo 
        command: bash -c "$(curl -L raw.githubusercontent.com/rafpcloud/k8s-the-hard-way/main/repo.sh)"
        register: mymotd
   